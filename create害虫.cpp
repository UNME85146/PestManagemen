#include <iostream>

#include "害虫.h"

int main() {
    std::cout << "=== 害虫管理系统测试 ===" << std::endl;

    // 1. 创建害虫对象
    std::cout << "\n1. 创建害虫对象：" << std::endl;
    std::vector<std::string> 初始特征 = { "嘴硬", "思想顽固", "喜欢吹牛" };
    害虫 害虫1("害虫", 初始特征, 1);
    害虫1.显示信息();

    // 2. 测试获取方法
    std::cout << "\n2. 测试获取方法：" << std::endl;
    std::cout << "名字: " << 害虫1.获取名字() << std::endl;
    std::cout << "年龄: " << 害虫1.获取年龄() << std::endl;
    std::vector<std::string> 特征列表 = 害虫1.获取特征();
    std::cout << "特征数量: " << 特征列表.size() << std::endl;

    // 3. 测试设置方法
    std::cout << "\n3. 测试设置方法：" << std::endl;
    害虫1.设置名字("害虫");
    害虫1.设置年龄(50);
    害虫1.显示信息();

    // 4. 测试年龄管理
    std::cout << "\n4. 测试年龄管理：" << std::endl;
    害虫1.增加年龄(1);
    std::cout << "增加1岁后，年龄: " << 害虫1.获取年龄() << std::endl;

    害虫1.减少年龄(1);
    std::cout << "减少1岁后，年龄: " << 害虫1.获取年龄() << std::endl;

    // 5. 测试行为方法
    std::cout << "\n5. 测试行为方法：" << std::endl;
    害虫1.行为();

    // 6. 测试红温值功能
    std::cout << "\n6. 测试红温值功能：" << std::endl;
    std::cout << "当前红温值: " << 害虫1.获取红温值() << std::endl;
    
    害虫1.增加红温值(50);
    std::cout << "增加50后，红温值: " << 害虫1.获取红温值() << std::endl;
    害虫1.显示信息();
    
    害虫1.增加红温值(60);
    std::cout << "\n增加60后，红温值: " << 害虫1.获取红温值() << std::endl;
    害虫1.显示信息();
    
    std::cout << "\n触发行为（红温值>100，应该触发禁言和踢人）：" << std::endl;
    害虫1.行为();

    // 7. 测试减少红温值
    std::cout << "\n7. 测试减少红温值：" << std::endl;
    害虫1.减少红温值(50);
    std::cout << "减少50后，红温值: " << 害虫1.获取红温值() << std::endl;
    害虫1.显示信息();

    // 8. 测试加入QQ群功能（会自动触发建政和吹牛）
    std::cout << "\n8. 测试加入QQ群功能：" << std::endl;
    害虫 害虫2("害虫2", {"爱建政", "爱吹牛"}, 2);
    害虫2.加入QQ群();

    // 9. 测试管理员权限功能
    std::cout << "\n9. 测试管理员权限功能：" << std::endl;
    std::cout << "当前管理员权限: " << (害虫1.获取管理员权限() ? "是" : "否") << std::endl;
    
    // 测试没有管理员权限时，红温值>100无法执行禁言和踢人
    std::cout << "\n测试场景1：没有管理员权限，红温值>100：" << std::endl;
    害虫1.增加红温值(60);  // 红温值变为60
    害虫1.增加红温值(50);  // 红温值变为110
    害虫1.行为();

    // 授予管理员权限
    std::cout << "\n授予管理员权限：" << std::endl;
    害虫1.设置管理员权限(true);
    害虫1.显示信息();

    // 测试有管理员权限时，红温值>100可以执行禁言和踢人
    std::cout << "\n测试场景2：有管理员权限，红温值>100：" << std::endl;
    害虫1.行为();

    // 测试单独调用禁言和踢人方法
    std::cout << "\n10. 测试单独调用禁言和踢人方法：" << std::endl;
    害虫 害虫3("害虫3", {"普通群友"}, 1);
    std::cout << "害虫3（无管理员权限）尝试禁言：" << std::endl;
    害虫3.禁言();
    std::cout << "害虫3（无管理员权限）尝试踢人：" << std::endl;
    害虫3.踢人();
    
    害虫3.设置管理员权限(true);
    std::cout << "\n害虫3（有管理员权限）尝试禁言：" << std::endl;
    害虫3.禁言();
    std::cout << "害虫3（有管理员权限）尝试踢人：" << std::endl;
    害虫3.踢人();

    // 11. 测试离婚和找女大行为
    std::cout << "\n11. 测试离婚和找女大行为：" << std::endl;
    害虫 害虫4("害虫4", {"爱离婚", "爱找女大"}, 3);
    std::cout << "测试离婚行为：" << std::endl;
    害虫4.离婚();
    std::cout << "\n测试找女大行为：" << std::endl;
    害虫4.找女大();

    // 12. 测试嫖娼行为
    std::cout << "\n12. 测试嫖娼行为：" << std::endl;
    害虫 害虫5("害虫5", {"爱嫖娼"}, 4);
    std::cout << "测试嫖娼行为：" << std::endl;
    害虫5.嫖娼();
    
    std::cout << "\n=== 生成害虫完毕 ===" << std::endl;

    return 0;
}